{
  "name": "goal-discovery-prototype",
  "version": "0.1.0",
  "description": "Throwaway prototype for testing goal discovery conversations",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "node -e \"const fs=require('fs');const sw=fs.readFileSync('sw.js','utf8');fs.writeFileSync('sw.js',sw.replace('__BUILD_TIMESTAMP__','v'+Date.now()))\"",
    "dev": "node --watch server.js",
    "test": "cross-env MOCK_MODE=true node --watch server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.37.0",
    "@supabase/supabase-js": "^2.97.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
